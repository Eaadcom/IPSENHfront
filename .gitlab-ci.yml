image: node:latest
stages:
  - install
  - test
  - build
  - deploy

#cache:
#  paths:
#    - ./node_modules

install:
  stage: install
  script:
    - npm install -g --cache --silent
#    - npm install -g @angular/cli
    - alias ng="~/node_modules/@angular/cli/bin/ng"
    - ng test

test:
  stage: test
  script:
#    - ng test
    - ng lint ipsenh-frontend

build:
  stage: build
  script:
    - echo "test"
#    - ng build --prod
  only:
    - master
    - develop

deploy:
  stage: deploy
  script:
    - echo "this is the deploy stage"
